{
  "name": "delete-all-firebase-auth-users",
  "version": "0.0.1",
  "description": "Deletes all Firebase Authentication users.",
  "main": "functions/lib/index.js",
  "scripts": {
    "build": "npm run clean && npm run compile",
    "clean": "rimraf functions/lib",
    "compile": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Pascal Luther",
  "license": "Apache-2.0",
  "dependencies": {
    "firebase-admin": "^8.0.0",
    "firebase-functions": "^2.3.1"
  },
  "devDependencies": {
    "typescript": "^3.5.2",
    "rimraf": "^2.6.3"
  }
}
